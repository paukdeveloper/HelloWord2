<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/boats_list_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/boats_list_arrow"
        android:layout_alignTop="@+id/boats_list_arrow"
        android:layout_toLeftOf="@+id/boats_list_arrow"
        android:layout_toStartOf="@+id/boats_list_arrow"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:maxLines="1"
        android:text="name" />

    <ImageView
        android:id="@+id/boats_list_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_arrow_right_144dp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/boats_list_arrow"
        android:background="@color/colorListDivider" />

</RelativeLayout>